#include "demo.h"

void demoNumbers ( istream& in, osteam& out)

{
	double answer = 0;
	double second = 0;
	double initial = 0;

	in >> initial; 
	while (!in.fail() )
	{
		initial = initial + second
	}
}